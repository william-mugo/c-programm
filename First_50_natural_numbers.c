// Display of first 50 natural numbers
# include <stdio.h>
int main ()
{
    int num;
    for(num=1;num<=50;num++)
    {
        printf(" %d ",num);
    }
    printf("\n for loop of body finished");
    return 0;
}